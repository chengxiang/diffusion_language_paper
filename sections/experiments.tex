% !TEX root = ../main.tex
\section{Experimental setup and main results}
\label{sec:experiments}
\paragraph{Tasks and models.}
We evaluate on the 1,319-problem GSM8K test set \citep{gsm8k} and the 500-problem MATH500 benchmark drawn from MATH \citep{math,math500}. GSM8K training uses 2.43 million solution rows combining GSM-style subsets of OpenMathInstruct-2, Orca-Math, and MetaMathQA \citep{openmath,orcamath,metamath}; the MATH model uses 3.97 million eligible MATH-family solution rows from OpenMathInstruct-2. All models use a 1,024-token canvas and the native Qwen tokenizer. The problem is supplied without demonstrations, followed by an instruction to reason step by step and put the final answer in a box. Final learned-prompt inference does not execute Qwen Transformer layers.

ELF-B has 12 blocks of width 768; ELF-L has 32 blocks of width 1,280. Their denoising paths use 90.44M and 638.15M parameters, respectively. Terminal-only decoder parameters add 156.52M and 157.05M, bringing the shared denoiser/decoder modules to 246.96M and 795.21M. The prompt encoders add 45.02M and 121.33M parameters, and both retain a frozen 388.96M-element Qwen lookup. The input and output vocabulary maps together account for 544.69M parameters (80.0\% of ELF-B; 41.7\% of ELF-L), separately tabulated in Appendix~\ref{app:literaturecontinuous}. Prompt encoding and its sparse token lookup occur once; the shared backbone runs at every denoising step and once more for terminal decoding. Appendix~\ref{app:implementation} gives the complete accounting.

\paragraph{Training and endpoint selection.}
We train the denoiser with teacher prompt conditioning for 12 epochs, then jointly adapt the learned prompt encoder and denoiser through epoch 18 for ELF-B and epoch 21 for ELF-L. The effective supervised batch is 512, with constant learning rate $0.002$, Muon and auxiliary optimizers, zero weight decay, and gradient clipping at one. We select NFT updates 300, 500, and 600 for GSM8K ELF-B, GSM8K ELF-L, and MATH ELF-L, respectively, using fixed development sets of 1,000 GSM-style and 500 MATH problems. Selection uses two-seed mean accuracy and favors the earlier update on an exact tie.

\paragraph{Evaluation.}
We first evaluate four sampling seeds at 32 and 64 steps. Each supervised/NFT pair uses a common step budget chosen by its equally weighted pilot mean pass@1; ties favor 32. All three pairs select 64 steps. Four further seeds give eight samples per problem at that budget. The stage and schedule ablations use 32 steps throughout. Main supervised endpoints select ELF/prompt EMA $0.9999/0.9999$; NFT selects $0.99/0.99$. The immediate prompt-swap experiment selects the standalone MSE prompt EMA $0.999$. The six-epoch controls below instead use ELF EMA $0.999$, with prompt EMA $0.999$ when applicable. GSM8K uses canonical numerical-answer scoring; MATH uses Math-Verify, with PRM800K retained as a secondary scorer \citep{mathverify,prm800k}.

We report mean pass@1 and the standard unbiased pass@$k$ estimator \citep{codex}. Confidence intervals use 4,000 paired problem-bootstrap replicates, retaining matched problems across conditions. They quantify uncertainty for fixed trained checkpoints and the observed sampling seeds. Appendix~\ref{app:evaluation} specifies the estimators, seed lists, and selection procedure.

\subsection{Reasoning performance}
\input{tables/main_results}
Table~\ref{tab:main} reports the six main endpoints. NFT raises mean pass@1 from 39.17\% to 42.16\% for GSM8K ELF-B, from 58.67\% to 63.74\% for GSM8K ELF-L, and from 18.85\% to 23.60\% for MATH ELF-L. The paired gains are $+2.99$ percentage points (95\% interval $[2.10,3.87]$), $+5.07$ ($[4.26,5.90]$), and $+4.75$ ($[3.33,6.15]$), respectively. All three intervals exclude zero. These contrasts compare the same problems and sampling seeds across stages; overlap between the separate absolute-accuracy intervals in the table does not determine uncertainty in the difference. The effect on oracle coverage is less uniform. At eight samples, GSM8K ELF-B changes from 67.32\% to 66.26\%, whereas MATH increases from 47.20\% to 52.60\%. Thus the benefit of post-training is not summarized fully by a single-sample score.

\input{tables/literature}
Table~\ref{tab:literature} compares continuous models using the original papers' accuracies and separates our pre-NFT and post-NFT results. Before reward-based fine-tuning, ELF-B's 39.17\% and ELF-L's 58.67\% on GSM8K already exceed the listed $\mathbb S$-FLM, FMLM+, and MLFM operating points. Pre-NFT ELF-B also reaches 34.57\% at 16 steps (17 backbone calls, seeds 42/123), versus FMLM+ Init's 33.6\% at 64 NFEs. The larger 7B TESS~2 reaches 66.6\% after GSM8K-specific fine-tuning at 100 steps with eight-shot prompting, above post-NFT ELF-L's 63.74\% with zero-shot prompting \citep{tess2}. Appendix~\ref{app:literaturecontinuous} retains both training stages' depth curves and the protocol differences; our sweep starts at eight steps. None of these four continuous comparators reports MATH500. Discrete masked and blockwise models are tabulated separately in Appendix~\ref{app:literaturediscrete}.

\subsection{Representation and early joint-training controls}
\label{sec:representationcontrols}
We compare four ELF-B training arms over six GSM8K epochs: learned projectors over layers 16/24/32; PCA over the same layers and widths $(256,256,512)$; a 1,024-dimensional PCA representation from layer 32; and the learned multilayer representation with a randomly initialized prompt encoder trained jointly from epoch zero. All representations are whitened. The first three arms use frozen Qwen prompt activations; the fourth retains Qwen's frozen token lookup and learns its prompt Transformer alongside ELF. All arms start from the same ELF initialization and use the same training population and effective batch.

\input{tables/four_arm_shared2}
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{figures/four_arm_shared2.pdf}
\caption{Four-arm GSM8K learning curves with shared $t=\tau^2$ inference, 32 steps, and ELF EMA $0.999$. Each point averages seeds 42/123 at one trained checkpoint; Table~\ref{tab:representation} expands epoch six to four seeds. Random joint training also selects prompt EMA $0.999$. Identity-clock counterparts appear in Appendix~\ref{app:fourarm}.}
\label{fig:fourarm}
\end{figure}
Table~\ref{tab:representation} and Figure~\ref{fig:fourarm} use the common inference clock $t=\tau^2$, equivalent to $(2,2,2)$ across multilayer groups. The table uses four generation seeds at epoch six; the learning curves retain two seeds at every epoch. At epoch six, learned projectors outperform PCA with the layers fixed by 2.33 percentage points (30.76\% versus 28.43\%; paired 95\% interval $[0.87,3.81]$). Under PCA, the multilayer representation exceeds layer 32 alone by 17.23 points (28.43\% versus 11.20\%; $[15.50,18.97]$). In the learning curves, learned multilayer leads from epoch two onward, while multilayer PCA exceeds layer-32 PCA at every evaluated epoch. These comparisons support both teacher-predictive projection learning and multilayer features within this training budget; they do not isolate the layer-selection effect under learned projectors or determine eventual performance ceilings.

Random joint training reaches 8.70\% at epoch six, 22.06 points below the teacher-prompt learned reference (paired 95\% interval $[20.24,23.81]$). The same epoch-six ordering holds with identity $t=\tau$ inference (Appendix~\ref{app:fourarm}), so the main representation and curriculum conclusions persist across both evaluated clocks.

% !TEX root = ../main.tex
\section{Literature comparisons and evaluation protocols}
\label{app:literature}
We separate continuous-state diffusion and flows, discrete masked or blockwise generation, and latent reasoners with an autoregressive decoder. This classification does not deny connections or hybrid constructions; it identifies the inference mechanism relevant to each comparison. External accuracies are reported by the cited papers, not generated by our harness. We prefer each method's original report and explicitly mark any secondary evaluation. In particular, a paper's \emph{MATH} result is not automatically a MATH500 result.

\subsection{Continuous methods: components, mechanisms, and NFE}
\label{app:literaturecontinuous}
Table~\ref{tab:literature} compares selected published operating points. A step-active parameter belongs to a component used repeatedly during denoising, including any recurrent vocabulary projection or embedding codebook. A terminal-only parameter is additional to those shared components. A one-time lookup can occupy substantial memory while requiring little arithmetic; parameter counts are therefore not FLOP estimates.

\begin{table}[ht]
\caption{Continuous formulations and their additional mechanisms. ``No terminal-only head'' means the vocabulary head already participates in denoising, not that vocabulary prediction is free.}
\label{tab:continuousmechanisms}
\centering\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{>{\raggedright\arraybackslash}p{1.65cm}>{\raggedright\arraybackslash}p{5.75cm}>{\raggedright\arraybackslash}p{5.7cm}}
\toprule
Method & Formulation and additional techniques & Recurrent versus one-time components\\
\midrule
$\mathbb S$-FLM & Hyperspherical transport; normalized architecture; truncated/adaptive schedule; top-1 velocity. & Released configuration: about 87.47M trunk/conditioning, 37.80M vocabulary head, 37.75M codebook. The head and codebook participate in the trajectory.\\
TESS 2 & Gaussian corruption of token-logit simplex; AR adaptation with UL2 masking objectives, label shifting, bidirectional attention, self-conditioning, and argmax projection during sampling. & Approximately 7B recurrent model. Input embedding mixture and vocabulary head are used at each step; no separate terminal-only head. Optional reward guidance adds a reward model and backward passes, but is not used for the reported GSM8K rows.\\
FMLM+ & Two-time flow maps; masking-style schedules; confidence-based posterior refinement. Scratch, MDLM initialization, and distillation are distinct training arms. & About 168M total (Table 11); the softmax head is recurrent. The paper does not report a separate trunk/head count. No additional terminal-only head.\\
MLFM & Continuous Brownian-bridge corruption with masking; SMDM adaptation through LoRA/AdaLN; context-corrupted guidance; discrete online token promotion. & 1,028M frozen parameters plus 319M adapters before merging. Vocabulary predictions and codebook operations occur during sampling. No separately reported trunk/head split.\\
Our ELF & Contextual latent flow; teacher-trained projectors and whitening; staged prompt encoder; self-conditioning/SCCFG; layer clocks; NFT. & 90.44M/638.15M denoising paths; 156.52M/157.05M terminal-only parameters. Prompt Transformer 45.02M/121.33M plus lookup 388.96M are used once. The terminal decoder reuses the backbone.\\
\bottomrule
\end{tabular}
\end{table}

The $\mathbb S$-FLM count is an analytical count of the released TinyGSM \texttt{small-sphere-arch} configuration (12 blocks, width 768, GELU MLP, vocabulary 49,152), rather than an exact count reported in the paper. Its release uses approximately 163M parameters including both vocabulary matrices. FMLM+'s approximately 168M is reported in Table~11. MLFM's 319M adapter count appears in Appendix~A; counting only its trainable adapters would omit the frozen network still evaluated at inference \citep{hyperspherical,posterior,mlfm}. Teacher resources are a further, separate axis: our Qwen Transformer is used during representation learning but is absent from final learned-prompt inference.

\begin{figure}[ht]
\centering
\includegraphics[width=\linewidth]{figures/continuous_literature_nfe.pdf}
\caption{GSM8K accuracy across continuous-model sampling budgets. Our curves use the same two seeds 42/123 throughout. FMLM+ values are the authors' Table~8; $\mathbb S$-FLM/MLFM Figure~1 and TESS~2 Figure~3b values are digitized and rounded to 0.1 percentage points \citep{posterior,hyperspherical,mlfm,tess2}. Left: ELF counts $S+1$ backbone calls for $S$ integration steps, including terminal decoding; the released $\mathbb S$-FLM sampler decodes on its last call. Right: MLFM retains its reported step axis because guidance, promotion, and early stopping prevent identifying steps with total NFEs. Lines connect measured operating points. Training data, solution format, model size, and scoring differ across papers.}
\label{fig:continuousnfe}
\end{figure}

\begin{table}[ht]
\caption{Numerical depth comparison: single-sample GSM8K accuracy (\%). $^\dagger$Approximate author-figure values, rounded to 0.1 points. Column $S$ denotes the reported budget: NFEs for the first four rows, sampling steps for MLFM, and integration steps for ELF. ELF therefore uses $S+1$ backbone evaluations.}
\label{tab:continuousnfe}
\centering\small
\begin{tabular}{lrrrr}
\toprule
Method & $S=8$ & $S=16$ & $S=32$ & $S=64$\\
\midrule
$\mathbb S$-FLM, top-1$^\dagger$ & 12.9 & 15.6 & 15.9 & 17.8\\
FMLM+, scratch & 8.7 & 13.4 & 19.0 & 19.1\\
FMLM+, Distill & 10.3 & 18.7 & 21.6 & 23.4\\
FMLM+, Init & 15.1 & 26.1 & 31.8 & 33.6\\
MLFM, CCFG + OTP$^\dagger$ & 20.8 & 24.0 & 26.7 & 27.1\\
\midrule
ELF-B + NFT, seeds 42/123 & 30.52 & 37.64 & 41.28 & 41.51\\
ELF-L + NFT, seeds 42/123 & 55.04 & 60.31 & 62.47 & 63.08\\
\bottomrule
\end{tabular}
\end{table}

Both TinyGSM-based papers execute generated Python solutions, with a 512-token canvas; our model generates natural-language solutions. $\mathbb S$-FLM's top-1 trajectory is evaluated at temperature one. Its 1,024-step Table~2 result is 18.0\%, while Figure~1 reaches approximately 18.2\% at 512 steps. MLFM reports 31.24\% at 256 steps, with its guidance scale selected from $\{0,2,4,6\}$. Its sampling algorithm contains two guidance fields and online promotion; we do not infer an exact NFE multiplier from the step label. These sources support a comparison across published protocols, not an isolated causal effect of architecture. ELF's current measured range starts at eight denoising steps, so it does not establish one- to four-step performance.

TESS~2's Table~3 reports 14.5\%/36.5\% GSM8K for its instruction-tuned v0.1/v0.3 models, and 66.6\%/59.2\% after GSM8K-symbolic fine-tuning, all at 100 diffusion steps. Appendix~D specifies eight-shot chain-of-thought prompting and last-number extraction, including for the task-finetuned setting. The 7B backbone is recurrent, unlike a one-time teacher or decoder. Figure~3b varies inference depth and reaches approximately 68.9\% at 1,000 steps. We retain this sweep separately: its plotted 100-step value is approximately 67.2\%, differing from Table~3's 66.6\%. These are author-reported operating points, not a matched evaluation against our model \citep{tess2}.

An earlier continuous precedent is the 1.3B Plaid adaptation in Diffusion of Thoughts, which reports 32.6\% on its GSM8K-Aug protocol \citep{dot}. This is a downstream adaptation reported by that study, not a GSM8K result in the original Plaid paper. We keep it separate from the recent full-answer curves rather than infer an unreported component split or matched NFE setting.

\subsection{Discrete masked and blockwise models}
\label{app:literaturediscrete}
Diffusion of Thoughts reports 53.5\% for discrete SEDD-medium on its GSM8K-Aug protocol, with separate self-consistency scores \citep{dot}. DiffuGPT-M reports 61.8\% with 355M parameters under a symbolic-reasoning protocol \citep{diffugpt}. These results are relevant precedents, but their compact reasoning format differs from our natural-language solution format. SMDM's 58.5\% likewise follows task-specific augmented-GSM8K fine-tuning \citep{smdm}. Duo's 1.7B result follows large-scale pretraining and supervised mathematical adaptation \citep{duoscaling}.

\begin{table}[ht]
\caption{Discrete masked and blockwise models, kept separate from continuous trajectories. Scores are percentages. ``MATH'' retains the original paper's label and is separate from MATH500. Values come from the cited method paper.}
\label{tab:literaturesmall}
\centering\small
\begin{tabular}{lrrrr}
\toprule
Model & Params (B) & GSM8K & MATH500 & MATH\\
\midrule
SMDM \citep{smdm} & 1.1 & 58.50 & --- & ---\\
Duo \citep{duoscaling} & 1.7 & 65.80 & --- & ---\\
OPDLM, 4B teacher \citep{opdlm} & 0.6 & 47.90 & 36.20 & ---\\
OPDLM \citep{opdlm} & 1.7 & 71.00 & 53.00 & ---\\
SDAR-Chat \citep{sdar} & 1.7 & 80.10 & 63.20 & ---\\
Simple-dLLM, masked \citep{simpledllm} & 0.6 & 29.30 & --- & 8.70\\
Simple-dLLM, block \citep{simpledllm} & 0.6 & 46.30 & --- & 12.90\\
TIDE-Cross \citep{tide} & 0.6 & 52.24 & --- & 13.20\\
OPDLM, self-distilled \citep{opdlm} & 0.6 & 42.10 & 28.00 & ---\\
Fast-dLLM v2 \citep{fastv2} & 1.5 & 62.00 & --- & 38.10\\
T3D--SDAR, block 4 \citep{t3d} & 1.7 & 70.96 & 47.00 & ---\\
T3D--SDAR, block 8 \citep{t3d} & 1.7 & 68.84 & 47.80 & ---\\
\bottomrule
\end{tabular}
\end{table}

TIDE's listed student is initialized from Qwen3-0.6B-Base and distilled across tokenizers from LLaDA2.0-mini; it uses zero-shot evaluation with 256 generation tokens and 256 steps \citep{tide}. Fast-dLLM v2 uses a 32-token block and eight-token sub-block, with greedy decoding and parallel decoding disabled for its principal accuracy table \citep{fastv2}. SDAR's principal results use zero-shot chain-of-thought prompting and four denoising steps for \emph{each four-token block} \citep{sdar}. OPDLM uses greedy static decoding with block size four; its 0.6B result changes substantially with teacher size, so the 4B-teacher result and self-distilled result are separate rows \citep{opdlm}. T3D's two-token-per-step setting is also local to successive blocks \citep{t3d}. None of these should be read as generating a whole answer in two or four model evaluations.

AR-converted diffusion models inherit their backbone's pretraining, while our model inherits teacher representations and synthetic supervision. Post-training token counts alone do not measure the complete resources underlying either approach.

\subsection{Reward-based reasoning and larger models}
\begin{table}[ht]
\caption{Reported reasoning results from post-training and larger diffusion systems. Scores are single-sample accuracies (\%). $L$ denotes generation length where specified. We keep one stated setting per row; the GSM8K and MATH500 maxima need not occur at the same length. All LLaDA rows use an 8B backbone.}
\label{tab:literaturerl}
\centering\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrl}
\toprule
Method & GSM8K & MATH500 & Setting / source table\\
\midrule
LLaDA-Instruct, d1 evaluation \citep{d1} & 78.2 & 36.2 & $L=512$, Table 1\\
d1 \citep{d1} & 82.1 & 40.2 & $L=512$, Table 1\\
wd1 \citep{wd1} & 82.3 & 39.0 & $L=512$, Table 1\\
wd1++ \citep{wd1} & 84.5 & 44.2 & Full tuning, Table 3\\
SFT + GDPO \citep{gdpo} & 84.99 & 41.4 & $L=512$, Table 2\\
SPG mixture \citep{spg} & 84.5 & 41.8 & $L=512$, Table 1\\
ESPO \citep{espo} & 83.7 & 43.4 & $L=512$, Table 1\\
d2 \citep{d2} & 85.0 & 41.6 & Task-specific, Table 6\\
SFT + IGPO \citep{igpo} & 86.4 & 47.4 & Greedy, Table 1\\
StableDRL \citep{stabledrl} & 86.3 & 44.0 & $L=512$, Table 1\\
GDSD \citep{gdsd} & 86.0 & 44.6 & $L=512$, Table 2\\
JustGRPO \citep{justgrpo} & 89.8 & 45.2 & $L=512$, Table 1\\
\midrule
SDAR-4B \citep{sdar} & 89.9 & 72.8 & Static block 4, Table 2\\
SDAR-8B \citep{sdar} & 91.3 & 78.6 & Static block 4, Table 2\\
StableDRL--SDAR-8B \citep{stabledrl} & 92.4 & 79.2 & Static sampling, Table 2\\
OPDLM-MATH-4B thinking \citep{opdlm} & 91.7 & 90.2 & Math-specialized, Table 5\\
OPDLM-MATH-8B thinking \citep{opdlm} & 93.8 & 92.4 & Math-specialized, Table 5\\
I-DLM-8B thinking \citep{idllm} & 95.0 & 96.8 & Up to 32,768 tokens, Table 1\\
I-DLM-32B thinking \citep{idllm} & 94.9 & 97.6 & Up to 32,768 tokens, Table 1\\
\bottomrule
\end{tabular}
\end{table}

Several distinctions matter when interpreting Table~\ref{tab:literaturerl}. LLaDA 1.5's original 42.6\% and LLaDOU's 44.6\% are labeled MATH in their original papers; they are not substituted for MATH500 here \citep{llada15,lladou}. JustGRPO's strongest setting uses left-to-right, one-token-per-step decoding of a diffusion-trained model \citep{justgrpo}. IGPO includes a length-aligned supervised stage before reinforcement learning \citep{igpo}. SDAR, TraDo, and their descendants can combine blockwise generation with long reasoning traces \citep{sdar,trado}. I-DLM preserves an AR-derived introspective consistency mechanism and enables thinking with a much larger token budget \citep{idllm}. These scores describe the performance frontier within discrete and blockwise generation; they are contextual references for our continuous model, with different inference mechanisms and budgets.

Other relevant post-training studies include MMaDA's mixed reasoning and UniGRPO, temporal consistency reinforcement, entropy-guided step selection, and curriculum initialization of the diffusion canvas \citep{mmada,midtruth,egspo,canvasanneal}. They motivate examining more than final training reward. In particular, CanvasAnneal reports different effects on GSM8K and MATH500; this is related evidence about task-dependent exploration, not a test of the all-correct-group filtering mechanism in our NFT implementation. We therefore treat our discarded-group measurements as diagnostics rather than an established explanation of cross-task gains.

\subsection{Inference, diversity, and alternative latent pipelines}
\begin{table}[ht]
\caption{Examples of other relevant evaluation settings. These rows describe different inference systems, rather than additional directly comparable backbone models. All scores are percentages.}
\label{tab:literatureinference}
\centering\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrrp{5.0cm}}
\toprule
System & GSM8K & MATH500 & Additional computation / protocol\\
\midrule
CDLM--Dream-7B \citep{cdlm} & 78.8 & --- & 8-shot GSM8K; mean 44.1 total steps; 256-token canvas.\\
CD4LM--LLaDA \citep{cd4lm} & 77.6 & --- & Mean 76.3 NFEs; 32-token blocks.\\
TCR--LLaDA \citep{midtruth} & 83.0 & 41.4 & Accuracy plus temporal-entropy RL reward; $L=512$.\\
LRD--Dream-Instruct \citep{lrd} & 82.7 & 41.8 & Latent refinement during token commitment; $L=512$.\\
MDPO + RCR \citep{mdpo} & --- & 44.2 & 256 steps; $L=512$; block size 128.\\
OPTD--LLaDA \citep{optd} & 78.85 & 34.0 & Native decoder; GSM/MATH tokens per forward 10.85/7.41.\\
IMH--LLaDA \citep{qualityexploration} & --- & 36.0 & Single-sample accuracy; the separate pass@$k$ score is 70.0.\\
IMH--WeDLM \citep{qualityexploration} & --- & 54.0 & Single-sample accuracy; the separate pass@$k$ score is 87.5.\\
LaDi-RL \citep{ladirl} & --- & 81.4 & Latent diffusion plus a 7B AR language backbone and extended reasoning.\\
AR plan + LLaDA \citep{arplan} & 87.2 & 41.8 & External Sonnet planner; 256-token diffusion completion.\\
\bottomrule
\end{tabular}
\end{table}

Training-free sampling, order learning, search, and adaptive stopping can materially alter a fixed diffusion checkpoint's mathematical performance \citep{fastdllm,unmasking,ots,sas,adas}. Consequently, an inference comparison should count all backbone evaluations, cached versus full-sequence work, guidance branches, final decoding, and any planner or verifier. Within our fixed implementation, total denoising steps remain a useful allocation axis; across architectures, equal step counts need not imply equal compute. Our timing analysis in Appendix~\ref{app:cost} complements that axis without redefining it.

LaDiR reports 84.8\% pass@1 on GSM8K and 46.2\% on its 500-example MATH evaluation, but it uses diffusion thought blocks with a LLaMA-3.1-8B language backbone and additional latent modules \citep{ladir}. LaDi-RL uses DeepSeek-R1-Distill-Qwen-7B for math plus latent modules and joint latent/text reward optimization \citep{ladirl}. In both systems, the language decoder is autoregressive: an 8B or 7B backbone label is neither the size of the diffusion module alone nor a one-time decoder cost. Separate component totals and full-answer NFEs are not established by those labels. We keep these systems separate from fully parallel continuous-token decoding. Related continuous representation studies include contextual AR rounding in CoDAR, continuous/discrete composition in CRoCoDiL, hierarchical latent generation in ColaDLM, and block-causal latent generation in AURORA-LM \citep{codar,crocodil,coladlm,auroralm}. Their principal evaluations address language modeling or generation rather than the paired mathematical benchmarks considered here.

The linked PlaidQ version~1 studies few-step and one-step continuous \emph{code} generation, not GSM8K or MATH500 \citep{plaidq}. Its later version is marked withdrawn for institutional disclosure requirements concerning research resources. We record it as a related coding preprint, without using it as a mathematical result. Its Table~3 reports 714.96M denoiser parameters, or 717.39M including the codebook and learned schedule; the inherited 440.47M Transformer trunk alone is not its complete recurrent footprint. The vocabulary projection participates in the continuous reconstruction at each step, so it is not a terminal-only head. Distribution-matching and paired-trajectory distillation are additional mechanisms; our depth sweep uses an undistilled model.
